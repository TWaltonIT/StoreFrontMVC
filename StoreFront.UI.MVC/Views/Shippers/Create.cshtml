@model StoreFront.DATA.EF.Models.Shipper

@{
    ViewData["Title"] = "Add New Shipper";
}

<section class="product_section gray-section layout_padding pb-4 pt-4 mb-4 text-center">
    <div class="heading_container heading_center">
        <h2>
            Add <span>Shipper</span>
        </h2>

    </div>
</section>

<div class="row">
    <div class="col-md-8 offset-md-2">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ShipperName" class="control-label"></label>
                <input asp-for="ShipperName" class="form-control" />
                <span asp-validation-for="ShipperName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShipperVillage" class="control-label"></label>
                <input asp-for="ShipperVillage" class="form-control" />
                <span asp-validation-for="ShipperVillage" class="text-danger"></span>
            </div>
        </form>
    </div>
</div>

<div class="form-group p-4 text-center">
    <input type="submit" value="Create" class="btn btn-primary" /> |
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
